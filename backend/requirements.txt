# Moonkeep Elite - Backend Dependencies
# Core API
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
websockets>=11.0

# Authentication & Security
PyJWT>=2.8.0
bcrypt>=4.0.0
slowapi>=0.1.9

# Networking & Packet Manipulation
scapy>=2.5.0
requests>=2.28.0

# Job Queue
rq>=1.0.0
redis>=5.0.0

# Recon-ng core (subset needed by recon_adapter subprocess bridge)
# Full recon-ng deps (flasgger, unicodecsv, dicttoxml, xlsxwriter, mechanize, lxml) are in
# requirements-recon.txt and only needed when running recon-ng directly.
pyyaml>=6.0
dnspython>=2.3.0
flask>=3.0.0
flask-restful>=0.3.10

# Utilities
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
httpx>=0.27.0
